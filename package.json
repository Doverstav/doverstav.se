{
  "scripts": {
    "prebuild": "rm -rf dist",
    "serve": "parcel src/static/index.html",
    "build": "parcel build src/static/index.html",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "cssnano": "^4.1.7",
    "typescript": "^3.1.3"
  }
}
